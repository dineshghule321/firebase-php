---
engines:
    duplication:
        enabled: true
        config:
            languages:
            - php
    fixme:
        enabled: true
    phan:
        enabled: true
    phpmd:
        enabled: true
ratings:
    paths:
        - "**.php"
        - "**.md"
exclude_paths:
    - build/
    - tests/
    - vendor/
